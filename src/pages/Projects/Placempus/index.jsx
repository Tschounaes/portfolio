import React from 'react';
import DevMessage from '../../../components/DevMessage';

const Placempus = () => {
    return (
        <DevMessage 
        title={'Placempus'}/>
    )
}

export default Placempus;